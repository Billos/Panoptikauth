{
  "name": "authentik-gotify-bridge",
  "version": "0.0.1",
  "description": "A bridge between Authentik and Gotify for notifications",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "semantic-release --no-ci",
    "release:dry": "semantic-release --dry-run"
  },
  "keywords": [
    "authentik",
    "gotify",
    "bridge",
    "notifications"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@codedependant/semantic-release-docker": "^5.1.1",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "semantic-release": "^25.0.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "express": "^5.2.1"
  }
}
